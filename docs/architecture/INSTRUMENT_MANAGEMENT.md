# Instrument Management (MF-4)

Instruments are **`Asset` rows** in category `MUSICAL_INSTRUMENT` with `InstrumentProfile`:

- `instrumentType`, `tuningNotes`, `maintenanceIntervalDays`

Same ownership, custody, assignment, and maintenance flows as all other assets.

# Operations Reporting (MF-7)

`GET /operations/reports` — catalog

`GET /operations/reports/:reportId` — JSON

Export: `GET /operations/reports/:reportId/csv|pdf` (`operations.report`)

Reports: operations, assignments, choir-utilization, protocol-utilization, conflicts, publications.

# Uniform Management (MF-4)

Uniforms are **`Asset` rows** in category `UNIFORM` with a `UniformProfile` extension:

- `size`, `gender`, `style`, `color`, `status` (`UniformAssetStatus`)

Assignments use **`AssetAssignment`** only — no separate uniform assignment tables.

Supports choir robes, protocol attire, badges, and name tags.
